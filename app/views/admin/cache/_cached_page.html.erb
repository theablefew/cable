<tr id="<%= dom_id(cached_page.location) %>">
  <!-- TODO This should loop through the attributes instead of depending on title. id, updated_at and created_at could be skipped. -->
  <th><%= cached_page.location.title %><br /><%= cached_page.location.url %></th>
  <td class="updated_at"><%= cached_page.timestamp.strftime('%B %d, %Y - %l:%M %p') %></td>
  <td class="actions">
    <%= link_to 'Clear Cache', refresh_cached_page_admin_cache_path( cached_page.location ), :confirm => 'Are you sure?', :class => "refresh_cache" %>
  </td>
</tr>    
